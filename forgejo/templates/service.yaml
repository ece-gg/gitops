apiVersion: v1
kind: Service
metadata:
  name: forgejo
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: forgejo
  ports:
    - name: http
      protocol: TCP
      port: 3000
